# 2026-05-14 SQL

## IFNULL vs COALESCE
Both functions are used to handle null values.

```sql
IFNULL(expression, alt_value)

COALESCE(val1, val2, ...., val_n)
```
IFNULL returns alt value if expression is null whereas COALESCE returns the first non-null value in the list. ISNULL is the same as IFNULL.

IFNULL: MySQL
ISNULL: SQLServer

So, in this way both IFNULL and COALESCE can be used to return an alt_value if the expression is null:
IFNULL(expression, alt_value) , COALESCE(expression, alt_value)

Which one to use?

Choosing the Right Function:
(ref: https://www.geeksforgeeks.org/sql/ifnull-vs-coalesce/)
- When dealing with a straightforward scenario involving two arguments and operating within a database system that supports these capabilities, utilize IFNULL/ISNULL.

- When you need to handle numerous possible NULL values or when you want to construct SQL that is more portable between different database systems, use COALESCE.

## INNER JOIN vs OUTER JOIN
LEFT, RIGHT, FULL are outer joins. They return both matching and non-matching records, with which side(s) is fully included in the result being the distinguishing factor between them.

INNER join only returns matching rows. 

### Why are they called inner and outer joins?
Related to the loop implementation of these joins: https://stackoverflow.com/questions/27211638/why-is-inner-join-and-outer-join-so-called


## FULL JOIN vs CARTESIAN PRODUCT
FULL JOIN gives matching and non-matching rows from both the tables (combination of LEFT and RIGHT join).

CROSS JOIN (or cartesian product) gives all possible permutations of rows (each row from table1 paired with each row of table2) regardless of matching. Number of output rows is the product of number of rows in each table.

## Order of execution
FROM  →  WHERE  →  GROUP BY  →  HAVING  →  SELECT  →  ORDER BY
(FWG-HSO)

## Correlated subquery
**Approach C — correlated subquery (works but slower on big data):**
```sql
SELECT d.name AS Department, e.name AS Employee, e.salary AS Salary
FROM   Employee e
JOIN   Department d ON e.departmentId = d.id
WHERE  e.salary = (
         SELECT MAX(salary) FROM Employee WHERE departmentId = e.departmentId
       );
```
- **Here MAX aggregate function is used without GROUP BY in the subquery, we are able to match the employee salary to department highest by just matching deptids?**

- What is a correlated subquery? : The subquery references a column from the outer query
- Why is this slow on bigger data? : inner query executed once for each outer query row, so for bigger data = more outer query rows = more inner query executions = slower.

## CTE
CTE : Common Table Expression

WITH x AS (...subquery...) -> CTE named x

_ref: https://www.geeksforgeeks.org/sql/cte-in-sql/_
A Common Table Expression (CTE) is a temporary result set in SQL that you can reference within a single query.